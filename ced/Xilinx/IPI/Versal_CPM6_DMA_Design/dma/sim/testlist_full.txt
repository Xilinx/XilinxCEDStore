# Full regression testlist -- run with: make regr TESTLIST=testlist_full.txt
#
# dma/ is the non-DDR, PL-AXI-backed (BRAM) Controller-1 variant, so only
# the "plaxi" DMA tests are listed here (the "ddr" DMA tests target the
# dma_ddr/ variant instead -- see dma_ddr/sim/testlist_full.txt).
#
# Source: uvma-pcie-sim-framework/cpm6/common/test/hdma/ (verified passing
# in cpm6/ctrl1ep_g6x8_hdma_1pf), now compiled in via
# sim/tb/test/test_pkg.svh + sim/tb/test/hdma/. The variant assignment
# below (plaxi -> dma/, ddr -> dma_ddr/) is based on naming convention
# and has NOT been independently re-run against this CED-generated
# project -- verify before relying on it for sign-off.
hello_world
test_s_hdma_plaxi_ctrlr1
test_M_bridge_plaxi_ctrlr1
test_M_bridge_plaxi_ctrlr1_4pf
test_M_bridge_plaxi_ctrlr1_4pf_axildecode
