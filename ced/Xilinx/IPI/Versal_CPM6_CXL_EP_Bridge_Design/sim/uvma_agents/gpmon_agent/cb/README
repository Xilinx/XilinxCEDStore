This directory contains common callback classes that a user can attach
to a gpmon agent. These do not come automatically built-in, you must
create your gpmon agent in your env, then create and attach your
callback class there as well. Users can also define their own callback
classes for their specific use case and still use the gpmon agent. 
